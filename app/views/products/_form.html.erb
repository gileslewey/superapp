<%#= form_with(model: product, local: true) do |form| %>
<%= form_for(@product) do |f| %
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, class: 'formLabel' %>
    <%= form.text_field :name, class: 'formField' %>
  </div>

  <div class="field">
    <%= form.label :description, class: 'formLabel' %>
    <%= form.text_area :description, id:, class: 'formField' %>
  </div>

  <div class="field">
    <%= form.label :image_url, class: 'formLabel' %>
    <%= form.text_field :image_url, id:, class: 'formField' %>
  </div>

  <div class="field">
    <%= form.label :grade, class: 'formLabel' %><br>
    <%= form.text_field :grade, id:, class: 'formField' %>
  </div>

  <div class="field">
    <%= form.label :price, class: 'formLabel' %>
    <%= form.text_area :price, id:, class: 'formField' %>
  </div>

  <div class="actions submit-btn">
    <%= form.submit class: 'btn btn-small' %>
  </div>
<% end %>
