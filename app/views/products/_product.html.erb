
<!--<% @products.each do |product| %>
    <div class="col-sm-2 col-md-4">
      <h1><%= link_to product.name, product %></h1>
      <p><%= product.description %></p>
      <strong>$ <%= product.price %></strong>
    </div>--><!-- /col -->
  <!--<% end %>-->
<% cache product do %>
<div class="productWrapper">
  <div class="col-xs-12 col-md-6 col-lg-4 coin-padding">
    <%= image_tag(product.image_url, class: "productImage img-constrain") %>
    <a href="#" id="productName"><h4><%= product.name %></h4></a>
    <p id="productDetails"><%= product.description %></p>
    <p id="productquality">Quality: <%= product.quality %></p>
    <p id="productPrice">Price: $<%= product.price %></p>
    <div class='btn btn-search'><%= link_to 'Show', product %></div> |
    <div class='btn btn-info'><%= link_to 'Edit', edit_product_path(product) %></div> |
    <div class='btn btn-warning'><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></div>
  </div><!-- /col -->
</div>
<% end %>
