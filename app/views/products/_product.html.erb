
<!--<% @products.each do |product| %>
    <div class="col-sm-2 col-md-4">
      <h1><%= link_to product.name, product %></h1>
      <p><%= product.description %></p>
      <strong>$ <%= product.price %></strong>
    </div>--><!-- /col -->
  <!--<% end %>-->

<div class="productWrapper">
  <div class="col-sm-2 col-md-4 coin-padding">
    <%= image_tag(product.image_url, class: "productImage img-responsive") %>
    <a href="#" id="productName"><h4><%= product.name %></h4></a>
    <p id="productDetails"><%= product.description %></p>
    <p id="productGrade"><%= product.grade %></p>
    <p id="productPrice"><%= product.price %></p>
    <%= link_to 'Show', product %>
    <%= link_to 'Edit', edit_product_path(product) %>
    <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %>
  </div><!-- /col -->
</div><!--/wrapper>
